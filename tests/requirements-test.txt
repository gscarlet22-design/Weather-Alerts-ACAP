# ── Test runner ───────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0

# ── HTTP mocking (intercepts requests calls without hitting real endpoints) ───
responses==0.25.3

# ── Time mocking (freeze datetime.now() calls in tests) ──────────────────────
freezegun==1.5.1

# ── Flask test client support ─────────────────────────────────────────────────
# Flask's own test client is used directly; no extra package needed.

# ── Indirect deps ─────────────────────────────────────────────────────────────
attrs==23.2.0
iniconfig==2.0.0
packaging==24.0
pluggy==1.5.0
