# ── Core HTTP client ──────────────────────────────────────────────────────────
# requests handles Digest authentication for VAPIX out-of-the-box via
# requests.auth.HTTPDigestAuth, which is required by most Axis devices.
requests==2.31.0

# ── Lightweight web framework for the local config UI ─────────────────────────
# Flask serves the configuration page on port 8080 inside the container.
# The Axis device port-forwards this if the runOptions expose the port,
# or you can reach it via SSH tunnel: ssh root@<device-ip> -L 8080:localhost:8080
flask==3.0.3

# ── Retry logic ───────────────────────────────────────────────────────────────
# urllib3 Retry + requests adapters give us exponential back-off on NWS calls.
urllib3==2.2.1

# ── Timezone handling ─────────────────────────────────────────────────────────
# NWS alert timestamps are RFC 3339 with timezone offsets. pytz handles display.
pytz==2024.1

# ── Indirect deps pinned for reproducibility ──────────────────────────────────
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.7
Werkzeug==3.0.3
Jinja2==3.1.4
MarkupSafe==2.1.5
click==8.1.7
itsdangerous==2.2.0
blinker==1.8.2
